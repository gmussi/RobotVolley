# Legacy

Pre-refactor single-file prototypes and the old Node static server live here for reference only.

| File | Description |
|------|-------------|
| `single-file-prototype.html` | Original monolithic game (all logic inline) |
| `index copy.html` | Backup copy of the prototype |
| `backup.html` | Older backup variant |
| `server.js` | Minimal static HTTP server (port 8177) — replaced by Vite |

Run the modern game with `npm run dev` from the project root.
